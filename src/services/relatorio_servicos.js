import { SortBy } from 'lodash';

export function ordenarPorNomeProfissionalAlfabeticamente(servicos) {
  SortBy(servicos.filter(evento => evento.horaEvento.match(new RegExp(`\d+\/${numeroDoSeuMes}\/\d+`), ['cliente.nome'])
}
